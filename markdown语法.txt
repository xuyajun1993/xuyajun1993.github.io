
Markdown新手入门指南

`**加粗**`、`*斜体*`、`~~删除线~~`


加粗：<b></b>，**加粗**`

就像当初我学Verilog一样，很容易就明白了。但到现在我还搞不清c语言。

这是简书的介绍:
献给写作者的 Markdown 新手指南

这是Markdown官方的语法说明，说实话我是看了半天不知其所以然，谁写的，一点都不利于普及嘛，简书的介绍就挺好，很容易就吸引人去用了。
Markdown 语法说明 (简体中文版)

其实总的来说放几个图就好了，简单又形象，学起来也快。
标题

很简单,就是几个#。
标题
段落

分段，第一段和第二段之间要有一行空。
分行不分段，在末尾连续两个空格，然后回车。
直接回车，在下一行写，会发现实际效果并没有分行，而是连续的，但在回车位置还留了一个空格，需注意。
分段
引用，缩进，列表

这是引用或缩进,用>或>>
引用
这是列表，无序的列表+，-，*都可以，有序的就是1.空格。 如果列表下又嵌套列表，那就得换用不同的符号了。
可循环使用，但符号之后的空格不能少，符号之前的空格也不能少。 列表

    嵌套列表
        嵌套列表
        嵌套列表
        嵌套列表
        嵌套列表
    嵌套列表

字体

三个—，三个***，就是一条分割线。粗体和斜体各有两个表示。下划线还是HTML表示。
字体
加黑效果：
加黑
链接

两种表示方法，[文字]+(链接 “说明”)表示，或者[文字]+[ID]；[ID]:link “说明”，在链接后加双引号，这是鼠标指向文字或图片时显示的信息，如下图：
链接

直接用<>标记链接或Email
链接
表格

在中间加:可以让文字居中或靠右，默认靠左。
表格
代码

代码块可以用三个`(tab键上面的那个),或者四个空格。单个的代码可以用两个`来标注。
代码
反斜杠

当你需要写一些标记符号，却又不想让它变成Markdown语言，怎么办？在前面加上反斜杠就行了。
反斜杠